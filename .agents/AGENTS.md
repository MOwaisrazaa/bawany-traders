# Workspace Coding Rules - Bawany Traders

## Version Control Constraints
*   **Do Not Push Code**: Under no circumstances should the agent run `git push` or deploy code directly to the remote repository. Always leave changes locally in the workspace so the user can review, commit, and push them manually.
